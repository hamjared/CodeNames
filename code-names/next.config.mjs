/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/CodeNames",
    assetPrefix: "/CodeNames",
    output: "export",
    reactStrictMode: true,
};

export default nextConfig;
